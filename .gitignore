# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/

# Environment variables
.env*.local

# Common ignores
node_modules
.next/
.DS_Store
dist/
*.log

# NOTE: `lib/` and `app/` were previously ignored here, but `lib/stockall/`
# holds real tracked source (stock-service, persistence). Git ignores only
# untracked files, so the existing code survived — but any NEW file added
# under lib/ would have been silently dropped. `app/` was deleted as dead
# code, so neither entry is needed anymore.
# 临时调试脚本
_*.cjs
